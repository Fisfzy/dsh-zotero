# 2026-08-28 ventus 恢复 + dsh-zotero 0.1.2-alpha.1 适配（✅ 完成）

## 最终状态（全部验证通过）

| 项 | 状态 |
|---|---|
| web | dsh-harness 源码版 **v0.1.2-alpha.1**（web-guard 守护自动拉起，10s 自愈 ✅） |
| dsh-ventus-plugins | **[active]**（bundles 注册 + 本地 client 兼容补丁） |
| dsh-zotero | **[active]**（标准 bundle 装配 + 0.1.2 inject 适配），`/api/status` 200 `source:local`（46ms） |
| GUI | 导航栏 对话/轨迹/上下文/科研/**Zotero** 齐全；面板绿点直连、库列表/检索/PDF 预览/设置可用 |
| dev_* 工具链 | 恢复（dev_plugin_status 可用，ventus 宿主 active） |
| 会话 | 重启自动续接（dsh-restart-recover）全程无手动干预 |

## 发生了什么（时间线）

1. 8/28 web 重启 → dsh-zotero 因注入器 disabled 块未装配（404）；ventus 被 [DISABLE-THIRDPARTY] 禁用 → dev_* 消失。
2. 用户要求：恢复 ventus + 更新到上游最新 + 适配 dsh-zotero 新版本。
3. 上游 ventus 更新 7c6c714→8edd220（16 commits）；其 **client.js 需要 dsh 新版客户服务 `conversationEvents`**，而当前 web（v0.1.2-alpha.1）没有 → client boot 3 pending（ventus/ego/cae 连锁）→ GUI 面板全挂。
4. 修复链：
   - **标准 bundle 装配**（不再依赖注入器）：插件 package.json `dsh.bundle` + `cordis.patch.yml`（name 包名锚点）+ profile bundles/dependencies 注册；`super-injector/registry.json` 清空防双注册。
   - **ventus 兼容补丁**：上游源码回退 7c6c714（与 0.1.2 匹配基线），lib/client.js 打判定补丁（`scripts/patch-ventus-client.mjs`：inject 移除 conversationEvents ×2 + register 调用判空 ×1）——**新版功能 + 本地兼容层**，上游适配后可删补丁。
   - **dsh-zotero 0.1.2-alpha.1 适配**（关键）：cordis 4.0.1 的可选服务 **`ctx.get('webServer')` 取不到**（隔离作用域）；官方姿势（dsh-cae-agent index.d.ts 注释："a nested ctx.inject was not firing"）＝ **把 webServer 声明进 `inject`**，作为 ctx 属性使用：`inject = ['tools','llm','webServer']` + `ctx.webServer`。启动诊断日志确认 `webServer present=true` → `route registered`。
   - 重复注册修复：dsh-better-sidebar 独立 bundle 与 ventus 内嵌版撞名（client-modules duplicate factory）→ 从 bundles 移除独立版，ventus 内嵌版独占。
   - web-guard 守护验证：3 次 kill 均 10s 内自动拉起；一次性鉴权 URL 从 guard 日志读取（`web-guard.log`）。

## 本地运维备忘

- 日常重启 web：`Stop-Process -Id (Get-NetTCPConnection -LocalPort 3081 ...).OwningProcess`；guard 10s 内拉起，新鉴权 URL 在 `C:\Users\Fisfzy\.dsh\web-guard.log` 尾部（`?token=`）。
- ventus client 补丁重放：`node scripts/patch-ventus-client.mjs`。

## 遗留/下一步

- ventus 8eddd220（科研工作台 2.0 等）待 **上游适配 dsh 0.1.2 后**删除本地补丁升级；git 对象已保留（8edd220 可查）。
- dsh-zotero 面板原功能（PDF 预览/读全文/概述/开读/设置热生效）已验证在线；**M3.2（独立文献会话 ZTR CHAT）** 待继续。
